"""
LLMBrain: heuristic brains written by an LLM (Claude) and improved by it after every
league round, from game statistics (see llm_report.py). Versioned: a version is never
changed once written; each improvement is a new module (v1.py, v2.py, ...) whose
docstring says what changed and why.
"""
